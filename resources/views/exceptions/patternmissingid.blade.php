<p>The pattern <code>{{ $error->patternClass }}</code> is missing the required <code>$id</code> property.</p>

<x-braid::elements.note>
    <p><b>Note:</b> If you have overridden the base <code>Pattern</code> constructor, remember to invoke <code>parent::__construct()</code> in the function body.</p>
</x-braid::elements.note>
